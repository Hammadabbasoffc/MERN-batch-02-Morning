## Question 1

Write a JavaScript program that prints **"Inside setTimeout"** after **5 seconds** using `setTimeout()`.

---

## Question 2

Write a program that prints the following output using two `setTimeout()` functions with different delays.

Start
End
Inside setTimeout with 200ms
Inside setTimeout

---

## Question 3

Create a **Promise** that resolves with the message:

Promise resolved successfully

Print the message using `.then()`.

---

## Question 4

Create a **Promise** that rejects with the message:

Promise rejected

Handle the error using `.catch()`.

---

## Question 5

Create a Promise that checks a variable `success`.

- If `success = true`, resolve the promise.
- If `success = false`, reject the promise.

Print the result using `.then()` and `.catch()`.

---

## Question 6

Write a function `delay()` that returns a **Promise** which resolves after **2 seconds**.

---

## Question 7

Create an `async` function `run()` that:

1. Prints `"Start"`
2. Waits for the `delay()` function
3. Prints `"After database connect"`

---

## Question 8

Explain what the `await` keyword does in an `async` function.  
Provide a simple example.

---

## Question 9

Write an `async` function that fetches data from the following API:

https://jsonplaceholder.typicode.com/posts/57

Print the response data in the console.

---

## Question 10

Modify the `getData()` function so that it prints **only the title of the post** instead of the entire object.

Example output:

Title: Example Post Title

---
