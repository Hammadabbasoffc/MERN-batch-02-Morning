// const arr1 = [1, 2, 3];

// const arr2 = [...arr1]


// console.log(`my first array ${arr1}`);
// console.log(`my copy array ${arr2}`);

// arr2.push(4);

// console.log(`my first array ${arr1}`);
// console.log(`my copy array ${arr2}`);








// const arr1 = [1, 2, 3];

// arr1.push(4);

// console.log(`my first array ${arr1}`);






// for of loop



/*

for (let value of array) {
   // code
}
*/

// for (let fruit of fruits) {
//     console.log(fruit);
// }








// let [firstFruit, ...remainingFruits] = fruits;

// console.log(`First fruit: ${firstFruit}`);
// console.log(`Second fruit: ${secondFruit}`);
// console.log(`Third fruit: ${thirdFruit}`);

// console.log(`First fruit: ${typeof firstFruit}`);
// console.log(`Remaining fruits: ${typeof remainingFruits}`);


// const fruits = ['apple', 'banana', 'cherry'];

// console.log(typeof fruits);








// Print all elements of an array using for loop.
// Print only even numbers from [1,2,3,4,5,6].
// Find the sum of all numbers in an array.
// Find the largest number in an array.

// Store first fruit in variable firstFruit.
// Store second fruit in variable secondFruit.
// Store remaining fruits in otherFruits.
// Skip the second element using destructuring.

// Difference between:
// arr2 = arr1
// arr2 = [...arr1]

// Why spread operator is shallow copy?
// Can we use spread in nested arrays safely?
// Why should we use const for arrays?



// let student = {
//     name: "Hammad",
//     age: 24,
//     department: "BS Information Technology",
//     isActive: true,
//     greet: function () {
//         console.log(`Hello, my name is ${this.name} and I am ${this.age} years old.`);
//     },
//     hobbies: ["coding", "gaming", "traveling"],
//     address: {
//         street: "123 Main St",
//         city: "Karachi",
//         country: "Pakistan"
//     }
// };

// console.log(student["name"]);

// console.log(student.hobbies[0]);


// for (let key in student) {
//     console.log(`${key}: ${student[key]}`);
// }

// spread operator in objects

// let student2 = { ...student };

// console.log(student.greet());



// Create an object with dynamic keys for multiple years using a loop.

// Predict the output of nested destructuring with default values.

// Explain why changing a nested property affects the original object when using the spread operator.

// Increment IDs in an array of objects without mutating the original array.

// Explain the output when calling an object method via a variable reference (this context issue).

// Modify a function that destructures nested objects so it works even if some nested properties are missing.

// Merge multiple objects with the spread operator while controlling which properties get overwritten.

// Rewrite a for-loop object assignment using computed property syntax in an object literal.

// Use destructuring to extract the first and last elements from an array inside an object in a single line.

// Given an array of objects with nested scores, create a new array with name and total score.

// Find the object with the highest nested property value using array and object methods.

// Explain the effect of Object.defineProperty flags like enumerable, writable, and configurable.

// Dynamically add properties to an object based on user input.

// Use object destructuring to swap values between two properties.

// Flatten a nested object into a single-level object with dot-separated keys.